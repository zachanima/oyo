<div class="element">
  <%= link_to 'Edit', edit_link_path(@page) %>
  <%= link_to 'Del', @page, :method => :delete, :confirm => 'Delete?' %>
  <h1><%= @page.title %></h1>
  <p><%= link_to @page.url, @page.url %></p>
</div>
